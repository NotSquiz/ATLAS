#!/usr/bin/env python3
"""
ATLAS Voice Bridge - Windows Launcher (no console)

This is the same as atlas_launcher.py but with .pyw extension
to hide the console window when double-clicked on Windows.

Usage: Double-click this file in Windows Explorer
"""

# Import and run the main launcher
from atlas_launcher import main

if __name__ == "__main__":
    main()
